const { Window } = require("./Window");

class ItemWindow extends Window {
    constructor(windowManager) {
        super(windowManager);
    }

    create() {
    }

    destroy() {
    }

    setup() {
    }

}

module.exports = {
    ItemWindow
};