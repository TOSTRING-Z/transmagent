# 服务端口（默认 3006）
PORT=3006

# 生产模式（启用静态文件服务 + HTTPS cookie）
NODE_ENV=production

# 会话密钥（务必修改为随机字符串）
SESSION_SECRET=change-me-to-a-random-string

# 管理员密码（首次启动时自动创建 admin 账号）
# 密码通过 bcrypt 哈希存储，修改此处后需删除 data/admins.json 重新生成
ADMIN_PASSWORD=admin123
